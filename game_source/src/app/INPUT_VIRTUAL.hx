package app;

// These map to VirtualInput elements, which are like buttons, but work directly with the Input Manager and mulitouch to allow
// for complicated virtual input setups

enum INPUT_VIRTUAL {
	// User defined virtual buttons
	JUMP;
	ATTACK;
}
