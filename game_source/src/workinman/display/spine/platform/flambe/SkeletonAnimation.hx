package workinman.display.spine.platform.flambe;

class SkeletonAnimation extends SkeletonSprite {

	public function new( skeletonData:SkeletonData, renderBoxes:Bool = false ) {
		super( skeletonData, renderBoxes );
	}
}
