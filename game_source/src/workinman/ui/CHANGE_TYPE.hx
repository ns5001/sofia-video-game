package workinman.ui;

// INTERFACE CHANGE EVENT [Buttons/UI/ScreenManager -> Main](events that come out of controls, buttons, keys, with the intention of changing something)
// These eventIDs are used by EInterfaceChange Class.
enum CHANGE_TYPE {
	OPEN_BEGIN;
	OPEN_COMPLETE;
	CLOSE_BEGIN;
	CLOSE_COMPLETE;
}
